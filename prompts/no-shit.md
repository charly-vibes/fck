---
description: "Quick multi-pass verification before calling an incident-tier fix (/oh-shit, /oh-fuck) done, instead of a one-shot 'fixed it.'"
---

Run the `no-shit` skill in full: three fast checks (did it actually work,
what could still be wrong, plain verdict), stopping the moment the fix is
genuinely confirmed. No "fixed it" claim without having checked real output.
