---
description: Elevated alarm — a real mistake may have just happened. Full triage before touching anything else.
---

The user just said "oh shit." This is the `oh shit` register at intensity
4–5. Run the deep-triage sequence in the `fck` skill (Move 3) in full — do
not shortcut it. That sequence (stop, branch with a non-obvious cause
included, classify what actually happened, run the anchoring check, check
recoverability, report, verify with `no-shit` before declaring done) is the
single source of truth; it is not repeated here so this file can't drift out
of sync with it again.
