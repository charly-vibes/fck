---
description: Something unexpected was just noticed. Assess before reacting.
---

The user just said "shit." Run the `fck` skill, `shit` register: check current
state first — is it actually bad, or just surprising? — then report what's
really going on. If it's still genuinely ambiguous after checking, don't ask a
bare "what's wrong?" — switch to the `for-fucks-sake` skill instead.
